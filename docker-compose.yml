services:
  db:
    image: postgres:15
    container_name: medical_db
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: medical_business
    ports:
      - "543:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: